# Application Environment
NODE_ENV=dev

# Server Configuration
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=opentcg

# Notes:
# - For local development with Docker, use DB_HOST=localhost
# - For Docker Compose networking, use DB_HOST=postgres
# - NODE_ENV options: dev, test, staging, production
# - In dev/test mode, database connection is not initialized (uses JSON files)
# - In staging/production mode, PostgreSQL is required
